"use client";

// TODO: implement form submit aduan
// Fields: original_text (textarea), wilayah (input), photo_url (file upload), latitude, longitude (dari map picker)
// POST ke /api/complaints/ via api.ts
// Redirect ke /complaint/{id} setelah berhasil

export default function ComplaintForm() {
  return <div>ComplaintForm — belum diimplementasi</div>;
}

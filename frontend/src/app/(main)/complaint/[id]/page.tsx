// TODO: detail aduan + NLP result — gunakan useComplaintDetail(id) + NLPResultCard
export default function ComplaintDetailPage({ params }: { params: { id: string } }) {
  return <div>Detail Aduan #{params.id} — belum diimplementasi</div>;
}
